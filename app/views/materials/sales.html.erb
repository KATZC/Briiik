
<%= render "shared/footer-sales" %>

<div class="container">

  <div class="row">
    <h3><strong>Annonces en ligne</strong></h3>
    <div class="col-sm-8 offset-sm-2">
      <div id="materials">
        <% @my_posts_online.each do |material| %>
          <div class="card-material-sales">
           <!--  <h4><%= material.description %></h4>
            <p><%= material.category %></p>
            <p><%= material.highest_bid %></p>
            <p><%= material.deadline %></p>
            <p><%= material.site.address%></p> -->
          <%= link_to material_path(material) do %>
            <%= render 'shared/card-material-sales-online', material: material %>
          <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row">
    <h3><strong>Matériau vendu à remettre à l'acheteur</strong></h3>
      <div class="col-sm-8 offset-sm-2">
        <div id="materials">
          <% @my_posts_pickup.each do |material| %>
            <div>
             <!--  <h4><%= material.description %></h4>
              <p><%= material.category %></p>
              <p><%= material.highest_bid %></p>
              <p><%= material.deadline %></p>
              <p><%= material.site.address%></p> -->
            <%= link_to material_path(material) do %>
              <%= render 'shared/card-material-sales-sold', material: material %>
            <% end %>
          </div>
          <% end %>
        </div>
      </div>
    </div>


    <div class="row">
    <h3><strong>Ventes passées</strong></h3>
      <div class="col-sm-8 offset-sm-2">
        <div id="materials">
          <% @my_posts_done.each do |material| %>
            <div>
             <!--  <h4><%= material.description %></h4>
              <p><%= material.category %></p>
              <p><%= material.highest_bid %></p>
              <p><%= material.deadline %></p>
              <p><%= material.site.address%></p> -->
            <%= link_to material_path(material) do %>
              <%= render 'shared/card-material-sales-historical', material: material %>
            <% end %>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>


<%= render "shared/footer-home" %>



