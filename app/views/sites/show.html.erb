<div class="container">
  <h2><%= @site.address %></h2>
    <h2><%= @site.company %></h2>

  <% @site_users.each do |user| %>
    <%= 'Créateur du chantier: ' %><%= user.email %>
  <% end %>

  <hr>

  <% if policy(@site).create? %>
    <%= simple_form_for([@site, @site_user]) do |f| %>
      <%= f.input :user_id, collection: @users, label:'Ajouter des compagnons :'%>
      <%= f.submit 'Ajouter', class: 'btn btn-primary' %>
    <% end %>
  <% end %>

  <br>
  <%= link_to 'Edit', edit_site_path(@site) %>
  <%= link_to 'Back', site_path %>
  <br>
</div>

<%= render "shared/footer-sales" %>

